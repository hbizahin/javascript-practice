const a = 10;
const b = 20;
const result = "The sum of " + a + " and " + b + " is " + (a + b);
console.log(result);
const math = `The sum of ${a} and ${b} is ${a + b}`;
console.log(math);

const challenge = `John Cena 
yah multiline is working
properly`;
console.log(challenge)

// object theke value nawa
const student = {name: 'Zahin', age: 22}
output = `My name is ${student.name} and I am ${student.age} years old`
